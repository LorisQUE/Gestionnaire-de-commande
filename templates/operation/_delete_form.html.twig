<form method="post" action="{{ path('operation_delete', {'id': operation.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette opération ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operation.id) }}">
    <button class="btn">Supprimer</button>
</form>
