{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ fournisseur.Libelle }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ fournisseur.Adresse }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ fournisseur.Ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ fournisseur.Pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fournisseur_index') }}">Retour à la liste</a>

    <a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}">Modifier</a>

    {{ include('fournisseur/_delete_form.html.twig') }}
{% endblock %}
