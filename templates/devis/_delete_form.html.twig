<form method="post" action="{{ path('devis_delete', {'id': devi.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce devis ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devi.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
