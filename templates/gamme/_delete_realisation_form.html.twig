<form method="post" action="{{ path('gamme_realisation_delete', {'id': gamme_realisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamme_realisation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
